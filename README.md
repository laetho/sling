# sling

cli tool for Slingboard

## Homebrew

This tap lives in the main repo (no `homebrew-` prefix), so use the full HTTPS URL.

```sh
brew tap laetho/sling https://github.com/laetho/sling
brew install sling
```
